#include <stdio.h>
#include <stdlib.h>
#define SIZE    10
void fillArray(int A[],int n);

int main()
{
    int Array[SIZE];
    fillArray(Array,SIZE);
    puts("Dato un array in MaxHeap")
    printf("Da")
    deleteNode(Array,)
}
